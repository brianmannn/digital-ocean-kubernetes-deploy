apiVersion: v1
kind: Service
metadata:
  name: do-kubernetes-sample-app
  namespace: default
  labels:
    app: do-kubernetes-sample-app
spec:
  type: NodePort
  selector:
    app: do-kubernetes-sample-app
  ports:
    - port: 80
      targetPort: http
      name: http
